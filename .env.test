# Weather Flick Admin Backend Test Environment Variables

# 기본 설정
DEBUG=true
ENVIRONMENT=testing

# 서버 설정
HOST=127.0.0.1
PORT=9001

# 데이터베이스 설정 (SQLite in-memory for tests)
DATABASE_URL=sqlite:///:memory:

# JWT 설정 (테스트용)
JWT_SECRET_KEY=test_secret_key_for_testing_only_do_not_use_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_MINUTES=1440
ALGORITHM=HS256

# 이메일 설정 (테스트용 - 실제로 전송하지 않음)
MAIL_SERVER=localhost
MAIL_PORT=1025
MAIL_USERNAME=test@example.com
MAIL_PASSWORD=testpassword
MAIL_FROM=test@example.com
MAIL_FROM_NAME=Weather Flick Test
MAIL_STARTTLS=false
MAIL_SSL_TLS=false

# Redis 설정 (테스트용)
REDIS_URL=redis://localhost:6379/15

# 외부 API 키 (테스트용 모킹)
KMA_API_KEY=test_kma_api_key
WEATHER_API_KEY=test_weather_api_key
KAKAO_API_KEY=test_kakao_api_key
NAVER_CLIENT_ID=test_naver_client_id
NAVER_CLIENT_SECRET=test_naver_client_secret
GOOGLE_API_KEY=test_google_api_key
KOREA_TOURISM_API_KEY=test_kto_api_key

# 프론트엔드 설정
ADMIN_FRONTEND_URL=http://localhost:5174